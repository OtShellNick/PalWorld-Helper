export type TCommandItem = {
  name: string;
  description: string;
};

export type TBaseRecord = Record<string, string>;

export type TPlayerInfo = {
  name: string;
  playeruid: string;
  steamid: string;
};
